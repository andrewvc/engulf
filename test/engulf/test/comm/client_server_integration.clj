(ns engulf.test.comm.client-server-integration
  (:require
   [engulf.core :as core]
   [engulf.comm.control :as ctrl]
   [engulf.comm.message :as cmsg]
   [lamina.core :as lc]
   [cheshire.core :as chesh]))

(core/-main)
